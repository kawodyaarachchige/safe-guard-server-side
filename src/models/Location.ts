export class Location{
    latitude: number;
    longitude: number;
    user: string

    constructor(latitude: number, longitude: number, user: string){ {
        this.latitude = latitude;
        this.longitude = longitude;
        this.user = user
    }
    }
}